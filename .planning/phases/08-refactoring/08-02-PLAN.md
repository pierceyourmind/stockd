---
phase: 08-refactoring
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - api.php
  - modules/stocks.php
  - modules/import.php
  - modules/alerts.php
  - modules/quotes.php
  - modules/dividends.php
  - modules/export.php
  - modules/analytics.php
autonomous: true

must_haves:
  truths:
    - "api.php is under 500 lines and acts only as a router dispatching to modules"
    - "Each module file contains its endpoint functions with require_once for lib/ dependencies"
    - "All 20 existing endpoints continue working without functional changes"
    - "Stock CRUD endpoints (list, get, create, update, delete) are in modules/stocks.php"
    - "Import endpoints (importCSV, dismissFlag, confirmRemoval) are in modules/import.php"
    - "Alert endpoints (alerts, createAlert, deleteAlert, checkAlerts) are in modules/alerts.php"
    - "Quote endpoints (quote, history, news, benchmark) are in modules/quotes.php"
    - "Dividend endpoints (dividends, portfolioDividends) are in modules/dividends.php"
    - "Export endpoint (export) is in modules/export.php"
    - "Empty analytics module placeholder exists at modules/analytics.php ready for phase 9+ endpoints"
  artifacts:
    - path: "modules/stocks.php"
      provides: "Stock CRUD endpoints"
      contains: "function listStocks"
    - path: "modules/import.php"
      provides: "CSV import and flag management endpoints"
      contains: "function importCSV"
    - path: "modules/alerts.php"
      provides: "Price alert endpoints"
      contains: "function listAlerts"
    - path: "modules/quotes.php"
      provides: "Quote, history, news, benchmark endpoints"
      contains: "function getQuote"
    - path: "modules/dividends.php"
      provides: "Dividend endpoints"
      contains: "function getDividends"
    - path: "modules/export.php"
      provides: "Export endpoint"
      contains: "function exportData"
    - path: "modules/analytics.php"
      provides: "Placeholder for analytics endpoints (phases 9-12)"
      contains: "Analytics endpoints"
    - path: "api.php"
      provides: "Router dispatching to modules"
      contains: "match.*action"
  key_links:
    - from: "api.php"
      to: "modules/stocks.php"
      via: "require_once before match dispatch"
      pattern: "require_once.*modules/stocks"
    - from: "modules/quotes.php"
      to: "lib/yahoo.php"
      via: "require_once for yahooContext"
      pattern: "require_once.*lib/yahoo"
    - from: "modules/import.php"
      to: "lib/csv-parsers.php"
      via: "require_once for parseCSV"
      pattern: "require_once.*lib/csv-parsers"
---

<objective>
Extract all endpoint functions from api.php into six module files and reduce api.php to a pure router under 500 lines.

Purpose: Complete the modular restructure. After plan 01 extracted shared utilities to lib/, this plan moves all 20 endpoint functions into domain-grouped module files. api.php becomes a thin router that loads dependencies and dispatches via the existing match() expression.

Output: Six module files in modules/, a router-only api.php under 500 lines, and all 20 endpoints working identically.
</objective>

<execution_context>
@/home/rob/.claude/get-shit-done/workflows/execute-plan.md
@/home/rob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-refactoring/08-01-SUMMARY.md
@api.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create module files with extracted endpoint functions</name>
  <files>modules/stocks.php, modules/import.php, modules/alerts.php, modules/quotes.php, modules/dividends.php, modules/export.php, modules/analytics.php</files>
  <action>
Create the `modules/` directory and seven files. Each file starts with `declare(strict_types=1);` and requires only the lib/ files it actually uses.

**modules/stocks.php** — 5 functions:
- `listStocks(PDO $pdo): never`
- `getStock(PDO $pdo): never`
- `createStock(PDO $pdo): never`
- `updateStock(PDO $pdo): never`
- `deleteStock(PDO $pdo): never`
- Requires: `lib/helpers.php` (for jsonResponse)

**modules/import.php** — 3 functions:
- `importCSV(PDO $pdo): never`
- `dismissFlag(PDO $pdo): never`
- `confirmRemoval(PDO $pdo): never`
- Requires: `lib/helpers.php` (for jsonResponse), `lib/csv-parsers.php` (for parseCSV, used by importCSV)

**modules/alerts.php** — 4 functions:
- `listAlerts(PDO $pdo): never`
- `createAlert(PDO $pdo): never`
- `deleteAlert(PDO $pdo): never`
- `checkAlerts(PDO $pdo): never`
- Requires: `lib/helpers.php` (for jsonResponse)

**modules/quotes.php** — 4 functions:
- `getQuote(): never`
- `getHistory(): never`
- `getNews(): never`
- `getBenchmark(): never`
- Requires: `lib/helpers.php` (for jsonResponse, findClosestPrice), `lib/yahoo.php` (for yahooContext)

**modules/dividends.php** — 2 functions:
- `getDividends(PDO $pdo): never`
- `portfolioDividends(PDO $pdo): never`
- Requires: `lib/helpers.php` (for jsonResponse), `lib/yahoo.php` (for yahooContext)

**modules/export.php** — 1 function:
- `exportData(PDO $pdo): never`
- Requires: `lib/helpers.php` (for jsonResponse)

**modules/analytics.php** — Empty placeholder:
- Add `declare(strict_types=1);`
- Add a comment: `// Analytics endpoints — populated in phases 9-12 (performance metrics, portfolio analytics, benchmarking)`
- No functions, no requires. This file exists to satisfy the modular structure so analytics endpoints have a home when they're implemented.

Use `require_once __DIR__ . '/../lib/helpers.php';` pattern (relative from modules/ up to project root, then into lib/). Each function is copied exactly from api.php — no logic changes, no signature changes, no behavior changes.

Important: The require_once paths in module files must use `__DIR__ . '/../lib/...'` since modules are in a subdirectory.
  </action>
  <verify>
Run `php -l modules/stocks.php && php -l modules/import.php && php -l modules/alerts.php && php -l modules/quotes.php && php -l modules/dividends.php && php -l modules/export.php && php -l modules/analytics.php` to confirm no syntax errors.
  </verify>
  <done>Seven module files exist in modules/ (six with endpoint functions, one analytics placeholder), each with declare(strict_types=1), appropriate lib/ requires. All pass PHP lint.</done>
</task>

<task type="auto">
  <name>Task 2: Reduce api.php to router-only</name>
  <files>api.php</files>
  <action>
Strip api.php down to just: bootstrap, auth, lib requires, module requires, CORS headers, and the match() router.

The final api.php should contain ONLY:
1. `declare(strict_types=1);`
2. `require_once __DIR__ . '/bootstrap.php';`
3. `require_once __DIR__ . '/auth/session.php';`
4. Require all lib/ files:
   ```php
   require_once __DIR__ . '/lib/helpers.php';
   require_once __DIR__ . '/lib/database.php';
   require_once __DIR__ . '/lib/yahoo.php';
   require_once __DIR__ . '/lib/csv-parsers.php';
   ```
5. Require all module files:
   ```php
   require_once __DIR__ . '/modules/stocks.php';
   require_once __DIR__ . '/modules/import.php';
   require_once __DIR__ . '/modules/alerts.php';
   require_once __DIR__ . '/modules/quotes.php';
   require_once __DIR__ . '/modules/dividends.php';
   require_once __DIR__ . '/modules/export.php';
   require_once __DIR__ . '/modules/analytics.php';
   ```
6. CORS headers (Content-Type, Allow-Origin, Allow-Methods, Allow-Headers)
7. OPTIONS early return
8. `requireAuth();`
9. `$pdo = getDatabase();`
10. `$action = $_GET['action'] ?? '';`
11. The match() expression dispatching all 20 actions to their functions (unchanged)

Remove ALL endpoint function definitions from api.php — they now live in modules/.

Verify the final api.php is under 500 lines (it should be approximately 40-50 lines).

Do NOT change the match() expression entries or the action names. The router must dispatch identically.
  </action>
  <verify>
1. Run `php -l api.php` — no syntax errors
2. Run `wc -l api.php` — must be under 500 lines (target: ~50 lines)
3. Verify all 20 actions present in match(): list, get, create, update, delete, importCSV, dismissFlag, confirmRemoval, quote, history, alerts, createAlert, deleteAlert, checkAlerts, news, benchmark, dividends, export, portfolioDividends
4. Start PHP server and test: `php -S localhost:8080` then curl a few endpoints to confirm they still respond
  </verify>
  <done>api.php is under 500 lines, contains only requires and router dispatch, all 20 endpoints work via module files, PHP lint passes.</done>
</task>

</tasks>

<verification>
1. `php -l api.php` — passes
2. `php -l modules/*.php` — all pass
3. `php -l lib/*.php` — all pass
4. `wc -l api.php` — under 500 lines
5. `grep -c "function " api.php` — should be 0 (no function definitions in router)
6. `grep -c "function " modules/*.php` — should total 19 (all 19 endpoint functions, noting exportData uses exit not never in some cases)
7. All 20 action strings present in api.php match() expression
8. No function definition exists in more than one file (no duplication)
</verification>

<success_criteria>
- api.php under 500 lines, acting as router only
- Seven module files in modules/ (six with endpoint functions, one analytics placeholder for phases 9-12)
- All lib/ files from plan 01 properly required by modules that need them
- All 20 endpoints respond identically to before refactoring
- No function definitions duplicated across files
- PHP lint passes on every file
</success_criteria>

<output>
After completion, create `.planning/phases/08-refactoring/08-02-SUMMARY.md`
</output>
