---
phase: 08-refactoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/database.php
  - lib/yahoo.php
  - lib/helpers.php
  - lib/csv-parsers.php
autonomous: true

must_haves:
  truths:
    - "Shared database setup (PDO init, WAL mode, migrations) lives in lib/database.php and returns a PDO instance"
    - "Yahoo Finance HTTP context creation lives in lib/yahoo.php as a reusable function"
    - "jsonResponse, cleanNumeric, and findClosestPrice helper functions live in lib/helpers.php"
    - "CSV parser functions (parseFidelityCSV, parseSchwabCSV, parseCSV) live in lib/csv-parsers.php"
    - "All existing endpoints continue working identically after lib extraction"
  artifacts:
    - path: "lib/database.php"
      provides: "PDO setup, migrations, connection"
      contains: "function getDatabase"
    - path: "lib/yahoo.php"
      provides: "Yahoo Finance HTTP context helper"
      contains: "function yahooContext"
    - path: "lib/helpers.php"
      provides: "jsonResponse, cleanNumeric, findClosestPrice"
      contains: "function jsonResponse"
    - path: "lib/csv-parsers.php"
      provides: "CSV parsing functions for Fidelity and Schwab"
      contains: "function parseCSV"
  key_links:
    - from: "api.php"
      to: "lib/database.php"
      via: "require_once and getDatabase() call"
      pattern: "require_once.*lib/database"
    - from: "api.php"
      to: "lib/helpers.php"
      via: "require_once"
      pattern: "require_once.*lib/helpers"
---

<objective>
Extract shared utilities from api.php into lib/ folder files.

Purpose: Create the foundation layer that module files will depend on. Database setup, Yahoo Finance HTTP helpers, response helpers, and CSV parsers are used across multiple endpoint groups and must be extracted first before modules can be separated.

Output: Four lib/ files containing all shared functions, with api.php updated to require them. All 20 endpoints must continue working identically.
</objective>

<execution_context>
@/home/rob/.claude/get-shit-done/workflows/execute-plan.md
@/home/rob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@api.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared utilities to lib/ folder</name>
  <files>lib/database.php, lib/yahoo.php, lib/helpers.php, lib/csv-parsers.php</files>
  <action>
Create the `lib/` directory and four files:

**lib/database.php** — Extract lines 20-139 from api.php (database setup + all migrations):
- Create a function `getDatabase(): PDO` that contains all the PDO initialization, WAL mode, busy_timeout, table creation (stocks, alerts, dividends), all ALTER TABLE migrations, the UNIQUE constraint migration, and the SnapTrade table drops.
- The function should accept no arguments and return the configured PDO instance.
- Keep `declare(strict_types=1);` at top.
- The `$dbPath` should use `__DIR__ . '/../db/stocks.db'` since it's now in lib/ subfolder.

**lib/yahoo.php** — Extract the repeated Yahoo Finance HTTP context creation:
- Create a function `yahooContext(int $timeout = 15): resource` that returns the `stream_context_create()` result with the standard User-Agent and GET method. This pattern is duplicated in getQuote (line 733), getHistory (line 912), getNews (line 1075), getBenchmark (line 1119), getDividends (line 1210), and portfolioDividends (line 1269).
- Keep `declare(strict_types=1);` at top.

**lib/helpers.php** — Extract three utility functions:
- `jsonResponse(array $data, int $status = 200): never` — from line 437-441
- `cleanNumeric(?string $value): ?float` — from lines 147-156
- `findClosestPrice(array $timestamps, array $closes, int $targetTime): ?float` — from lines 859-890
- Keep `declare(strict_types=1);` at top.

**lib/csv-parsers.php** — Extract CSV parsing functions:
- `parseFidelityCSV(string $csvContent): array` — from lines 164-239
- `parseSchwabCSV(string $csvContent): array` — from lines 248-340
- `parseCSV(string $csvContent): array` — from lines 347-408
- Add `require_once __DIR__ . '/helpers.php';` since these use `cleanNumeric()`.
- Keep `declare(strict_types=1);` at top.

All functions keep their exact signatures, exact logic, exact return values. No behavioral changes.
  </action>
  <verify>
Run `php -l lib/database.php && php -l lib/yahoo.php && php -l lib/helpers.php && php -l lib/csv-parsers.php` to confirm no syntax errors.
  </verify>
  <done>Four lib/ files exist, each with declare(strict_types=1), and each passes PHP lint. Functions have identical signatures and logic to their originals in api.php.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire api.php to use lib/ files</name>
  <files>api.php</files>
  <action>
Update api.php to require the new lib/ files and remove the extracted code:

1. After the existing `require_once __DIR__ . '/auth/session.php';` line, add:
   ```php
   require_once __DIR__ . '/lib/helpers.php';
   require_once __DIR__ . '/lib/database.php';
   require_once __DIR__ . '/lib/yahoo.php';
   require_once __DIR__ . '/lib/csv-parsers.php';
   ```

2. Replace the entire database setup block (lines 20-139, from `$dbPath = __DIR__...` through the SnapTrade DROP TABLE statements and closing `catch`) with:
   ```php
   $pdo = getDatabase();
   ```

3. Remove the extracted function definitions from api.php:
   - Remove `cleanNumeric()` function (lines 147-156)
   - Remove `parseFidelityCSV()` function (lines 164-239)
   - Remove `parseSchwabCSV()` function (lines 248-340)
   - Remove `parseCSV()` function (lines 347-408)
   - Remove `jsonResponse()` function (lines 437-441)
   - Remove `findClosestPrice()` function (lines 859-890)

4. In each function that creates a Yahoo Finance `$context`, replace the `stream_context_create([...])` block with `$context = yahooContext();` (or `yahooContext(10)` for getNews which uses a 10-second timeout). Functions to update: `getQuote()`, `getHistory()`, `getNews()`, `getBenchmark()`, `getDividends()`, `portfolioDividends()`.

5. Keep the CORS headers, OPTIONS handling, `requireAuth()` call, the `match()` router, and ALL endpoint functions in api.php. Do NOT move endpoint functions yet — that happens in plan 02.

The match() expression and all 20 endpoint functions must remain exactly as they are (minus the extracted helpers and Yahoo context creation).
  </action>
  <verify>
Run `php -l api.php` to confirm no syntax errors. Then start the PHP server with `php -S localhost:8080` and test a few endpoints:
- `curl -s http://localhost:8080/api.php?action=list` should return stocks JSON (may need auth cookie)
- Verify api.php line count is significantly reduced from 1386 (should be roughly 900-1000 lines now)
  </verify>
  <done>api.php requires all four lib/ files, uses getDatabase() for PDO, uses yahooContext() for HTTP contexts, and all extracted functions are removed. PHP lint passes. All 20 endpoints remain in api.php and work identically.</done>
</task>

</tasks>

<verification>
1. `php -l api.php` — no syntax errors
2. `php -l lib/database.php && php -l lib/yahoo.php && php -l lib/helpers.php && php -l lib/csv-parsers.php` — all pass
3. `wc -l api.php` — significantly reduced from 1386 lines
4. All 20 actions still present in api.php match() expression
5. No function definition duplication between api.php and lib/ files
</verification>

<success_criteria>
- Four lib/ files created with extracted shared utilities
- api.php uses require_once for all lib/ files
- api.php calls getDatabase() instead of inline PDO setup
- api.php calls yahooContext() instead of inline stream_context_create
- All extracted functions removed from api.php (no duplication)
- PHP lint passes on all files
- All 20 endpoint functions still present and unchanged in api.php
</success_criteria>

<output>
After completion, create `.planning/phases/08-refactoring/08-01-SUMMARY.md`
</output>
