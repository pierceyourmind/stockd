---
phase: 05-snaptrade-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api.php
  - index.php
  - .env
  - composer.json
  - composer.lock
  - auth/snaptrade_callback.php
  - test_snaptrade.php
autonomous: true

must_haves:
  truths:
    - "No SnapTrade code exists in api.php (no routes, functions, table creation, or SDK references)"
    - "No SnapTrade UI exists in index.php (no brokerage connections section, state properties, or methods)"
    - "composer.json contains no snaptrade-php-sdk dependency"
    - "auth/snaptrade_callback.php and test_snaptrade.php files do not exist"
    - "Database has no connections, positions, sync_log, snaptrade_users, or accounts tables"
    - ".env contains no SNAPTRADE_* environment variables"
    - "App loads, authenticates, and displays existing stocks without errors"
  artifacts:
    - path: "api.php"
      provides: "Clean API with no SnapTrade code"
      contains: "match ($action)"
      must_not_contain: "snaptrade|SnapTrade|brokerage_connection"
    - path: "index.php"
      provides: "Clean frontend with no SnapTrade UI"
      must_not_contain: "brokerageConnections|connectBrokerage|snaptradeConnect|brokerage-connections"
    - path: "composer.json"
      provides: "Dependencies without SnapTrade SDK"
      contains: "vlucas/phpdotenv"
      must_not_contain: "snaptrade-php-sdk"
    - path: ".env"
      provides: "Environment config without SnapTrade credentials"
      contains: "AUTH_PASSWORD_HASH"
      must_not_contain: "SNAPTRADE_CLIENT_ID|SNAPTRADE_CONSUMER_KEY|SNAPTRADE_USER_ID"
  key_links:
    - from: "api.php"
      to: "bootstrap.php"
      via: "require_once"
      pattern: "require_once.*bootstrap\\.php"
    - from: "api.php"
      to: "auth/session.php"
      via: "require_once and requireAuth()"
      pattern: "requireAuth\\(\\)"
---

<objective>
Remove all SnapTrade code, dependencies, database tables, and configuration from the codebase.

Purpose: Clean the codebase of the abandoned SnapTrade API integration so Phase 6 (CSV Import Engine) starts from a clean foundation with no dead code or unused dependencies.
Output: A working app with all SnapTrade artifacts removed. Existing manual stock entry, auth gate, quotes, charts, alerts, benchmarks, and dividends continue to function.
</objective>

<execution_context>
@/home/rob/.claude/get-shit-done/workflows/execute-plan.md
@/home/rob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove SnapTrade backend code, files, and dependencies</name>
  <files>api.php, .env, composer.json, composer.lock, auth/snaptrade_callback.php, test_snaptrade.php</files>
  <action>
1. **Delete SnapTrade-specific files:**
   - Delete `auth/snaptrade_callback.php`
   - Delete `test_snaptrade.php`

2. **Remove SnapTrade code from api.php:**
   - Remove the SnapTrade table creation block (lines 124-192): the CREATE TABLE statements for `connections`, `positions`, `sync_log`, `snaptrade_users`, and `accounts`. Keep the closing `} catch (PDOException $e)` block that ends the database init try block (line 193-195).
   - Remove the three SnapTrade route entries from the match statement (lines 216-218): `snaptradeConnect`, `snaptradeConnections`, `snaptradeAccounts`.
   - Remove SnapTrade helper functions (lines 229-249): `getSnapTradeClient()` and `getSnapTradeUser()`.
   - Remove SnapTrade endpoint functions (lines 952-1070): `snaptradeConnect()`, `snaptradeConnections()`, and `snaptradeAccounts()`. These are the last functions in the file, so after removal the file should end after the `exportData()` function.

3. **Drop SnapTrade database tables:**
   Add a one-time migration in the database init section of api.php (after the existing table creation statements, before the catch block). Use DROP TABLE IF EXISTS for: `connections`, `positions`, `sync_log`, `snaptrade_users`, `accounts`. This is safe because these tables only contain SnapTrade data. After the migration has been run once, it can optionally be left in place as a no-op safety net (IF EXISTS means it won't error on subsequent runs).

4. **Clean .env file:**
   - Remove the `SNAPTRADE_CLIENT_ID` line
   - Remove the `SNAPTRADE_CONSUMER_KEY` line
   - Remove the `SNAPTRADE_USER_ID` line
   - Remove the comment lines about SnapTrade (lines 5, 9)
   - Keep `AUTH_PASSWORD_HASH` and `APP_URL` (APP_URL is still useful for general app config)

5. **Uninstall SnapTrade SDK via Composer:**
   - Run `composer remove konfig/snaptrade-php-sdk` in the project root
   - This updates composer.json and composer.lock and removes vendor files
   - Verify `vlucas/phpdotenv` remains in composer.json (it should, since only snaptrade-php-sdk is being removed)
  </action>
  <verify>
  - `grep -ri snaptrade api.php` returns no results
  - `grep -ri snaptrade .env` returns no results
  - `grep snaptrade composer.json` returns no results
  - `test ! -f auth/snaptrade_callback.php && echo "deleted"` prints "deleted"
  - `test ! -f test_snaptrade.php && echo "deleted"` prints "deleted"
  - `php -l api.php` returns "No syntax errors detected"
  - `php -r "require 'bootstrap.php';"` runs without error (composer autoload works)
  </verify>
  <done>
  All SnapTrade backend code, files, SDK dependency, and env vars are removed. api.php has no syntax errors. bootstrap.php/composer autoload still works with phpdotenv.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove SnapTrade frontend code and verify app functionality</name>
  <files>index.php</files>
  <action>
1. **Remove Brokerage Connections HTML section from index.php:**
   - Remove the entire `<!-- Brokerage Connections -->` section (lines 1252-1308): the `<section class="brokerage-connections">` block including all its child templates for connections list, accounts list, empty state, and loading state.

2. **Remove SnapTrade state properties from Alpine.js stockApp():**
   - Remove `brokerageConnections: []` (line 1837)
   - Remove `brokerageAccounts: []` (line 1838)
   - Remove `connectionsLoading: false` (line 1839)

3. **Remove SnapTrade-related init() code:**
   - Remove the OAuth URL parameter handling block in init() (lines 1896-1913): the `connected === 'success'` check and the `error` parameter handling with the errorMap containing 'connection_failed', 'csrf_failed', 'no_snaptrade_user', 'storage_failed'.
   - Remove the `await this.loadBrokerageConnections();` call from init() (line 1918). Keep the surrounding lines (`await this.loadBenchmarks()` above and `this.startAutoRefresh()` below).

4. **Remove SnapTrade methods from Alpine.js:**
   - Remove `loadBrokerageConnections()` method (lines 1940-1961)
   - Remove `connectBrokerage()` method (lines 1963-1965)
   - Remove `reconnectBrokerage()` method (lines 1967-1969)

5. **Verify no orphaned references remain:**
   - Search index.php for any remaining references to: `brokerage`, `snaptrade`, `connectBrokerage`, `reconnectBrokerage`, `brokerageConnections`, `brokerageAccounts`, `connectionsLoading`, `loadBrokerageConnections`
   - If any found, remove them.
  </action>
  <verify>
  - `grep -ri snaptrade index.php` returns no results
  - `grep -i 'brokerageConnections\|brokerageAccounts\|connectionsLoading\|connectBrokerage\|reconnectBrokerage\|loadBrokerageConnections\|brokerage-connections' index.php` returns no results
  - Start the PHP dev server (`php -S localhost:8000`) and verify:
    - The page loads without JavaScript console errors (check with curl that HTML is served)
    - The API responds to `curl http://localhost:8000/api.php?action=list` (may require auth -- if so, just verify the PHP server starts without fatal errors)
  - `php -l index.php` returns no syntax errors
  </verify>
  <done>
  All SnapTrade frontend code removed from index.php. No brokerage connection UI, state, or methods remain. App serves pages and API responds without errors. Existing stock cards, charts, alerts, benchmarks, and dividends are unaffected.
  </done>
</task>

</tasks>

<verification>
Complete phase verification:

1. **No SnapTrade code anywhere:**
   - `grep -ri snaptrade api.php index.php .env composer.json` returns empty
   - `auth/snaptrade_callback.php` does not exist
   - `test_snaptrade.php` does not exist

2. **No SnapTrade database tables:**
   - `php -r "$pdo = new PDO('sqlite:db/stocks.db'); $tables = $pdo->query(\"SELECT name FROM sqlite_master WHERE type='table'\"); foreach($tables as \$t) echo \$t['name'].PHP_EOL;"` shows only: stocks, alerts, dividends (no connections, positions, sync_log, snaptrade_users, accounts)

3. **App functions correctly:**
   - `php -l api.php` -- no syntax errors
   - `php -l index.php` -- no syntax errors
   - Core tables (stocks, alerts, dividends) still exist and contain data
   - Auth gate (session.php, login.php, logout.php) still functional
</verification>

<success_criteria>
- CLEAN-01: All SnapTrade code, dependencies, database tables, and environment variables removed
- CLEAN-02: Composer dependency konfig/snaptrade-php-sdk uninstalled, vlucas/phpdotenv retained
- CLEAN-03: auth/snaptrade_callback.php and test_snaptrade.php deleted
- App continues to function: auth gate works, stock CRUD works, quotes/charts/alerts/benchmarks load
</success_criteria>

<output>
After completion, create `.planning/phases/05-snaptrade-removal/05-01-SUMMARY.md`
</output>
