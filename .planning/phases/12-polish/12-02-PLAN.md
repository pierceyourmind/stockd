---
phase: 12-polish
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - index.php
  - modules/analytics.php
autonomous: true

must_haves:
  truths:
    - "User sees loading spinner with descriptive text during historical backfill operation"
    - "User sees loading spinner with descriptive text during sector enrichment operation"
    - "User can select date ranges 1M, 3M, 6M, 1Y, All for the historical portfolio chart"
    - "Return calculations display a clear, specific disclaimer explaining what is and is not included"
  artifacts:
    - path: "index.php"
      provides: "Enhanced loading states, expanded date range buttons, improved return disclaimer"
      contains: "3m"
    - path: "modules/analytics.php"
      provides: "Enhanced disclaimer text with specific dividend impact explanation"
      contains: "dividend"
  key_links:
    - from: "index.php date range buttons"
      to: "filteredSnapshots() method"
      via: "selectDateRange sets dateRange state, filteredSnapshots filters by cutoff"
      pattern: "selectDateRange.*3m"
    - from: "index.php return disclaimer"
      to: "modules/analytics.php getReturns()"
      via: "API response includes disclaimer field"
      pattern: "returnDisclaimer"
    - from: "index.php backfill button"
      to: "triggerBackfill method"
      via: "aria-busy and descriptive loading text during operation"
      pattern: "aria-busy.*backfillStatus"
---

<objective>
Add loading state improvements, expand date range selector, and enhance return calculation labels for better UX clarity.

Purpose: UX-01 (loading indicators), UX-02 (date range selector), UX-03 (return labels) requirements. These three small UX refinements make the analytics section more usable and set correct expectations about return calculations.
Output: Enhanced index.php with better loading states, more date range options, and clearer disclaimers.
</objective>

<execution_context>
@/home/rob/.claude/get-shit-done/workflows/execute-plan.md
@/home/rob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-polish/12-RESEARCH.md
@index.php
@modules/analytics.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance loading states for backfill and sector enrichment</name>
  <files>index.php</files>
  <action>
Improve loading indicators for the two long-running operations: historical backfill and sector enrichment.

**1. Backfill button loading state** (around line 1707-1712):

Replace the current backfill button with an enhanced version that includes aria-busy and more descriptive text:
```html
<button @click="triggerBackfill()" :disabled="backfillStatus === 'loading'" :aria-busy="backfillStatus === 'loading'" class="primary">
    <span x-show="backfillStatus !== 'loading'">Backfill History</span>
    <span x-show="backfillStatus === 'loading'">
        <span class="loading" style="width: 16px; height: 16px; display: inline-block; vertical-align: middle; margin-right: 8px;"></span>
        Backfilling... this may take 1-2 minutes
    </span>
</button>
```

**2. Sector enrichment loading state:**

Find the "Enrich Sectors" button in the Allocation section (rendered in the allocation toggle area). Currently it uses `allocationLoading`. Add a dedicated `enrichingLoading` boolean state property to Alpine (near the existing `allocationLoading` property around line 2552):
```javascript
enrichingLoading: false,
```

Find where enrichSectors is called in the frontend. Look for the `enrichSectors` fetch call. If it exists as a method, enhance it. If it is part of `loadAllocationData()`, add a separate trigger. The key change is: wherever the enrich sectors button exists, add:
- `:aria-busy="enrichingLoading"` attribute
- `:disabled="enrichingLoading"` attribute
- Loading text: "Enriching sectors... fetching data from Yahoo Finance"
- A small spinner inline via `<span class="loading">` when loading

If there is no standalone "Enrich Sectors" button in the UI (it might be auto-triggered), then instead enhance the allocation loading state: when `allocationLoading` is true, show "Loading allocation data..." with a spinner in the allocation section container.

**3. Analytics section loading state** (around line 1700 area):

Find the analytics loading indicator (shown when `analyticsLoading` is true). Enhance it to show a spinner and descriptive text:
```html
<div x-show="analyticsLoading" style="text-align: center; padding: 40px;">
    <span class="loading" style="width: 32px; height: 32px; display: inline-block; margin-bottom: 12px;"></span>
    <p style="color: var(--pico-muted-color);">Loading analytics data...</p>
</div>
```

Do the same for the allocation loading state — when `allocationLoading` is true, show a spinner with text "Loading allocation data..." in the allocation section.
  </action>
  <verify>
Run: `php -l index.php` — no syntax errors.
Open the app, click "Historical Analytics" toggle — should show "Loading analytics data..." with spinner.
Click "Backfill History" — should show spinner with "Backfilling... this may take 1-2 minutes" text and aria-busy attribute (inspect with DevTools).
Click "Allocation & Income" toggle — should show "Loading allocation data..." with spinner during fetch.
  </verify>
  <done>
Loading indicators display descriptive text and spinners during backfill, sector enrichment, and analytics loading. All loading buttons include aria-busy for accessibility. Users see clear feedback about what is happening and approximately how long it takes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Expand date range selector and enhance return disclaimer</name>
  <files>index.php, modules/analytics.php</files>
  <action>
**Part A: Expand date range buttons**

In the date-range-buttons div (around line 1747-1752), replace the current buttons with the expanded set:
```html
<div class="date-range-buttons">
    <button @click="selectDateRange('1m')" :class="{ active: dateRange === '1m' }">1M</button>
    <button @click="selectDateRange('3m')" :class="{ active: dateRange === '3m' }">3M</button>
    <button @click="selectDateRange('6m')" :class="{ active: dateRange === '6m' }">6M</button>
    <button @click="selectDateRange('1y')" :class="{ active: dateRange === '1y' }">1Y</button>
    <button @click="selectDateRange('all')" :class="{ active: dateRange === 'all' }">All</button>
</div>
```

This removes the 1W and YTD buttons and adds 3M, 6M, 1Y. The requirement specifies: 1M, 3M, 6M, 1Y, All.

Update the `filteredSnapshots()` method (around line 3428-3441) to handle the new ranges:
```javascript
filteredSnapshots() {
    let cutoff = 0;
    const now = Date.now();

    if (this.dateRange === '1m') {
        cutoff = now - (30 * 86400000);
    } else if (this.dateRange === '3m') {
        cutoff = now - (90 * 86400000);
    } else if (this.dateRange === '6m') {
        cutoff = now - (180 * 86400000);
    } else if (this.dateRange === '1y') {
        cutoff = now - (365 * 86400000);
    }
    // 'all' keeps cutoff at 0 (show everything)

    return this.historicalSnapshots.filter(s => (s.snapshot_date * 1000) >= cutoff);
},
```

Update the `renderHistoricalChart()` method: In the Chart.js options, update the x-axis time unit to be dynamic based on date range. Currently it is hardcoded to 'day'. Change it to use a helper or inline logic:
- For '1m': unit 'day'
- For '3m': unit 'day'
- For '6m': unit 'week'
- For '1y': unit 'month'
- For 'all': unit 'month'

Inside the chart options scales.x.time section, replace the static `unit: 'day'` with a dynamic value based on `this.dateRange`. The simplest approach is to compute it before creating the chart:
```javascript
const timeUnit = ({'1m': 'day', '3m': 'day', '6m': 'week', '1y': 'month', 'all': 'month'})[this.dateRange] || 'day';
```
Then use `unit: timeUnit` in the chart config.

Since the chart is destroyed and recreated on each range change (existing pattern), this will work correctly.

**Part B: Enhance return calculation disclaimer**

In modules/analytics.php, find the `getReturns()` function (around line 351). Update the disclaimer text in the response to be more specific and informative:

Replace the current disclaimer:
```
'Price return only. Does not include dividends, fees, or the effect of deposits/withdrawals. For tax reporting, use your broker statements.'
```

With this enhanced version:
```
'Returns shown are price-only (capital gains/losses). They do not include dividends received, brokerage fees, or the impact of deposits and withdrawals. For dividend-paying stocks, actual total returns are typically 2-4% higher annually. These figures may differ from your broker statements, which often show time-weighted or money-weighted returns. For tax reporting, always use your broker-provided documents.'
```

In index.php, find the static return-disclaimer div in the allocation section (around line 1928). Check if it has a hardcoded disclaimer. If so, update it with the same enhanced text. The existing dynamic disclaimer (line 1742, bound to `returnDisclaimer`) will automatically use the updated API text.

Also verify that the CSS for `.return-disclaimer` (around line 1313) has the amber left-border styling from the research. If it only has basic styling, enhance it to:
```css
.return-disclaimer {
    font-size: 0.8rem;
    color: var(--pico-muted-color);
    margin-bottom: 24px;
    font-style: italic;
    background: rgba(255, 193, 7, 0.1);
    padding: 12px;
    border-left: 3px solid #ffc107;
    border-radius: 4px;
}
```

If this styling already exists, leave it. Only add what is missing.
  </action>
  <verify>
Run: `php -l index.php && php -l modules/analytics.php` — no syntax errors.
Open the app, toggle Historical Analytics:
1. Verify date range buttons show: 1M, 3M, 6M, 1Y, All (no 1W or YTD)
2. Click each range button — chart filters data correctly
3. 6M and 1Y ranges use appropriate time units (week/month labels on x-axis)
4. Return disclaimer text is the enhanced version mentioning dividends, 2-4%, and broker documents
5. Disclaimer has amber left-border styling
  </verify>
  <done>
Date range selector shows 1M, 3M, 6M, 1Y, All with appropriate chart time units per range. Return disclaimer is specific about what is excluded (dividends, fees, deposits/withdrawals) with a note that actual total returns are typically higher for dividend stocks. Loading states from Task 1 and these UX improvements complete all Phase 12 UX requirements.
  </done>
</task>

</tasks>

<verification>
1. PHP syntax: `php -l index.php && php -l modules/analytics.php` — all pass
2. Loading states: Backfill button shows spinner + "this may take 1-2 minutes" during operation
3. Loading states: Analytics and allocation sections show spinner + descriptive text during load
4. Date range: 5 buttons visible (1M, 3M, 6M, 1Y, All), each filters chart data correctly
5. Date range: Chart x-axis labels use day/week/month units appropriately per range
6. Disclaimer: Enhanced text visible mentioning dividends, 2-4% annual impact, and broker document reference
7. Accessibility: Loading buttons include aria-busy attribute
</verification>

<success_criteria>
- UX-01 satisfied: Loading indicators shown during historical backfill and sector enrichment
- UX-02 satisfied: Date range selector works for historical chart (1M, 3M, 6M, 1Y, All)
- UX-03 satisfied: Return calculations labeled clearly with explanations matching user expectations
- No regressions: Existing chart rendering, analytics loading, and allocation features still work
</success_criteria>

<output>
After completion, create `.planning/phases/12-polish/12-02-SUMMARY.md`
</output>
